name: gzc-pretty
version: !include "../../version.yaml"

library:
  source-dirs: src

default-extensions: !include "../../default-extensions.yaml"

dependencies:
- base >= 4.7 && < 5
- ansi-wl-pprint
- gzc-core
- text

ghc-options:
- -Wall
- -Wextra
