name: gzc-core
version: !include "../../version.yaml"

library:
  source-dirs: src

default-extensions: !include "../../default-extensions.yaml"

dependencies:
- base >= 4.7 && < 5
- diagnose
- text
- nsc-core

ghc-options:
- -Wall
- -Wextra
