public record monoid
  (0 a : type) :=

  val mempty : a

public open {{monoid}}

/--
  Additive monoid on unsigned 64-bis integer.
-/
public let +-monoid-u64 
  : monoid u64 :=
  
  @{ let mempty := 0 }

/--
  Multiplicative monoid on unsigned 64-bis integer.
-/
public let ×-monoid-u64 
  : monoid u64 :=

  @{ let mempty := 1 }
